interface {{ interface.name }}
  {% for ip in interface.ip_addresses.all() -%}
    address {{ ip }}
  {% endfor %}
  {% if interface.label -%}nameif {{ interface.label }}{% endif -%}
  {% if device.cf.security_level -%}security-level {{ device.cf.security_level }}{% endif -%}
  no shut
!
